# Инструкция по деплою на Render.com

## Шаг 1: Создание репозитория на GitHub

1. Зайдите на https://github.com
2. Нажмите "New repository" (зеленая кнопка или + → New repository)
3. Название репозитория: `telegram-giveaway-bot` (или любое другое)
4. Выберите **Public** или **Private**
5. НЕ ставьте галочки на "Initialize with README"
6. Нажмите "Create repository"

## Шаг 2: Подключение проекта к GitHub

После создания репозитория GitHub покажет инструкции. Выполните в терминале:

```bash
cd /Users/danial/Desktop/розыгрыш

# Подключите ваш репозиторий (замените USERNAME на ваш GitHub username)
git remote add origin https://github.com/USERNAME/telegram-giveaway-bot.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

**Если потребуется авторизация:**
- GitHub больше не поддерживает пароли для HTTPS
- Используйте Personal Access Token (Settings → Developer settings → Personal access tokens)
- Или настройте SSH ключи

## Шаг 3: Деплой на Render.com

1. **Зарегистрируйтесь** на https://render.com
   - Можете использовать GitHub аккаунт (быстрее)

2. **Создайте новый Web Service:**
   - В Dashboard нажмите "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий
   - Выберите репозиторий `telegram-giveaway-bot`

3. **Настройки сервиса:**
   - **Name:** `telegram-giveaway-bot` (или любое название)
   - **Region:** Frankfurt (Европа) или любой другой
   - **Branch:** `main`
   - **Root Directory:** оставьте пустым
   - **Runtime:** `Python 3`
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python3 bot.py`
   - **Plan:** Free (или Starter за $7/месяц для более стабильной работы)

4. **Добавьте переменные окружения:**
   - В разделе "Environment" нажмите "Add Environment Variable"
   - **Key:** `BOT_TOKEN`
   - **Value:** ваш токен бота от @BotFather
   - Нажмите "Save Changes"

5. **Создайте сервис:**
   - Нажмите "Create Web Service"
   - Render начнет деплой

## Шаг 4: Проверка работы

1. Дождитесь окончания деплоя (обычно 2-3 минуты)
2. Проверьте логи в разделе "Logs"
3. Отправьте `/start` боту в Telegram
4. Если все работает - бот готов!

## Важные моменты

- ✅ Render автоматически перезапускает бота при падении
- ✅ Логи доступны в реальном времени в интерфейсе Render
- ✅ При каждом push в GitHub бот автоматически обновится
- ⚠️ На Free плане сервис может "засыпать" после 15 минут без активности
- 💡 Для 24/7 работы лучше использовать Starter план ($7/месяц) или VPS

## Команды для обновления бота

Когда нужно обновить код:

```bash
cd /Users/danial/Desktop/розыгрыш
git add .
git commit -m "Описание изменений"
git push origin main
```

Render автоматически перезапустит бота с новым кодом!

