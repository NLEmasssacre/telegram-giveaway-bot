# Инструкция по развертыванию бота на сервере

## Рекомендуемые сервисы для России 🇷🇺

### 1. **Timeweb Cloud** (РЕКОМЕНДУЕТСЯ ДЛЯ РОССИИ) ⭐⭐⭐
- ✅ Российский провайдер, работает стабильно
- ✅ Принимает российские карты
- ✅ VPS от 200₽/месяц
- ✅ Простая панель управления
- ✅ Техподдержка на русском
- 🔗 https://timeweb.com/cloud
- 💰 От 200₽/месяц (VPS)

### 2. **Selectel** ⭐⭐
- ✅ Российский провайдер
- ✅ VPS и облачные сервисы
- ✅ Принимает российские карты
- ✅ Хорошая техподдержка
- 🔗 https://selectel.ru
- 💰 От 300₽/месяц

### 3. **Beget** ⭐⭐
- ✅ Российский хостинг
- ✅ VPS от 200₽/месяц
- ✅ Принимает российские карты
- ✅ Простая панель управления
- 🔗 https://beget.com/ru/vps
- 💰 От 200₽/месяц

### 4. **Hetzner Cloud** (Европа, но работает) ⭐
- ✅ Немецкий провайдер, принимает карты
- ✅ Дешево: от €4/месяц (~400₽)
- ✅ Высокая производительность
- ✅ Работает из России
- 🔗 https://www.hetzner.com/cloud
- 💰 От €4/месяц (~400₽)

### 5. **Contabo** (Европа) ⭐
- ✅ Немецкий провайдер
- ✅ Очень дешево: от €3/месяц
- ✅ Принимает карты
- 🔗 https://contabo.com
- 💰 От €3/месяц (~300₽)

**Как деплоить:**
1. Зарегистрируйтесь на railway.app
2. Подключите GitHub репозиторий
3. Railway автоматически определит Python проект
4. Добавьте переменную окружения `BOT_TOKEN`

---

### 2. **Render.com**
- ✅ Бесплатный план доступен
- ✅ Простой деплой из GitHub
- ✅ Автоматический перезапуск
- 🔗 https://render.com
- 💰 Бесплатно (с ограничениями) или $7/месяц

**Как деплоить:**
1. Зарегистрируйтесь на render.com
2. Создайте новый "Web Service"
3. Подключите GitHub репозиторий
4. Команда запуска: `python3 bot.py`
5. Добавьте переменную окружения `BOT_TOKEN`

---

### 3. **PythonAnywhere**
- ✅ Специализируется на Python
- ✅ Бесплатный план для начинающих
- ✅ Простой интерфейс
- 🔗 https://www.pythonanywhere.com
- 💰 Бесплатно (ограничения) или $5/месяц

---

### 4. **VPS (Hetzner Cloud)** - для продвинутых
- ✅ Полный контроль
- ✅ Дешево (~€4/месяц)
- ✅ Высокая производительность
- 🔗 https://www.hetzner.com/cloud
- 💰 От €4/месяц

---

## Подготовка проекта

### 1. Создайте репозиторий на GitHub

```bash
cd /Users/danial/Desktop/розыгрыш
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_USERNAME/розыгрыш.git
git push -u origin main
```

### 2. Убедитесь, что файлы созданы:
- ✅ `Procfile` - для Railway/Render
- ✅ `requirements.txt` - зависимости Python
- ✅ `.gitignore` - чтобы не коммитить .env и логи
- ✅ `.env` НЕ должен быть в Git (уже в .gitignore)

---

## Настройка переменных окружения

На сервере нужно добавить переменную окружения:

**`BOT_TOKEN`** = ваш токен бота от @BotFather

---

## Деплой на Timeweb Cloud (РЕКОМЕНДУЕТСЯ ДЛЯ РОССИИ)

### Шаги:

1. **Зарегистрируйтесь** на https://timeweb.com/cloud
2. **Создайте VPS сервер:**
   - Выберите тариф (минимум 1GB RAM, 1 CPU)
   - ОС: Ubuntu 22.04 LTS
   - Регион: Москва или Санкт-Петербург
3. **Подключитесь к серверу по SSH:**
   ```bash
   ssh root@ваш_ip_адрес
   ```
4. **Установите зависимости:**
   ```bash
   apt update && apt upgrade -y
   apt install python3 python3-pip python3-venv git -y
   ```
5. **Клонируйте проект:**
   ```bash
   git clone https://github.com/ВАШ_USERNAME/розыгрыш.git
   cd розыгрыш
   ```
6. **Создайте виртуальное окружение:**
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
   ```
7. **Создайте .env файл:**
   ```bash
   nano .env
   # Добавьте: BOT_TOKEN=ваш_токен
   ```
8. **Настройте автозапуск** (см. раздел ниже)

---

## Деплой на Railway.app (альтернатива, если доступен)

### Шаги:

1. **Зарегистрируйтесь** на https://railway.app
2. **Подключите GitHub** в настройках аккаунта
3. **Создайте новый проект** → "New Project"
4. **Deploy from GitHub repo** → выберите ваш репозиторий
5. **Добавьте переменную окружения:**
   - Settings → Variables → Add Variable
   - Name: `BOT_TOKEN`
   - Value: ваш токен бота
6. **Railway автоматически определит Python** и установит зависимости
7. **Бот запустится автоматически!**

---

## Деплой на Render.com

### Шаги:

1. Зарегистрируйтесь на https://render.com
2. Создайте "New → Web Service"
3. Подключите GitHub репозиторий
4. Настройки:
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python3 bot.py`
   - **Plan:** Free (или Starter за $7/месяц)
5. В разделе "Environment" добавьте:
   - `BOT_TOKEN` = ваш токен
6. Нажмите "Create Web Service"

---

## После деплоя

### Проверка работы:
1. Отправьте `/start` боту в Telegram
2. Проверьте логи на сервере (Railway/Render показывают логи в интерфейсе)

### Мониторинг:
- Railway: Dashboard → ваш проект → Metrics/Logs
- Render: Dashboard → ваш сервис → Logs

---

## Важные замечания

1. **Не коммитьте `.env` файл** - он уже в `.gitignore`
2. **Добавьте токен как переменную окружения** на сервере
3. **Проверьте, что `story_image.png` загружен** в репозиторий
4. **Логи доступны** в интерфейсе сервиса

---

## Настройка автозапуска на VPS (systemd)

После установки на VPS нужно настроить автозапуск, чтобы бот работал постоянно:

**Создайте systemd сервис:**
```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Python и зависимостей
sudo apt install python3 python3-pip python3-venv -y

# Клонирование проекта
git clone https://github.com/ВАШ_USERNAME/розыгрыш.git
cd розыгрыш

# Создание виртуального окружения
python3 -m venv venv
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt

# Создание .env файла
echo "BOT_TOKEN=ваш_токен" > .env

# Запуск через systemd (для автозапуска)
sudo nano /etc/systemd/system/telegram-bot.service
```

**1. Создайте файл сервиса:**
```bash
sudo nano /etc/systemd/system/telegram-bot.service
```

**2. Вставьте следующее содержимое:**
```ini
[Unit]
Description=Telegram Giveaway Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/розыгрыш
Environment="PATH=/root/розыгрыш/venv/bin"
ExecStart=/root/розыгрыш/venv/bin/python3 bot.py
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

**3. Активируйте и запустите сервис:**
```bash
sudo systemctl daemon-reload
sudo systemctl enable telegram-bot.service
sudo systemctl start telegram-bot.service
```

**4. Проверьте статус:**
```bash
sudo systemctl status telegram-bot.service
```

**5. Просмотр логов:**
```bash
sudo journalctl -u telegram-bot.service -f
```

---

## Быстрая установка на Timeweb (скрипт)

Создайте файл `setup.sh` в проекте:

```bash
#!/bin/bash
# Обновление системы
apt update && apt upgrade -y

# Установка Python и зависимостей
apt install -y python3 python3-pip python3-venv git

# Клонирование проекта (замените на ваш репозиторий)
git clone https://github.com/ВАШ_USERNAME/розыгрыш.git
cd розыгрыш

# Создание виртуального окружения
python3 -m venv venv
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt

# Создание .env файла
echo "BOT_TOKEN=ваш_токен_здесь" > .env

# Создание systemd сервиса
cat > /etc/systemd/system/telegram-bot.service << EOF
[Unit]
Description=Telegram Giveaway Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=$(pwd)
Environment="PATH=$(pwd)/venv/bin"
ExecStart=$(pwd)/venv/bin/python3 bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
EOF

# Активация сервиса
systemctl daemon-reload
systemctl enable telegram-bot.service
systemctl start telegram-bot.service

echo "✅ Бот установлен и запущен!"
```

---

## Рекомендация для России 🇷🇺

**Лучший вариант: Timeweb Cloud VPS**
- ✅ Российский провайдер, работает стабильно
- ✅ Принимает российские карты
- ✅ Техподдержка на русском
- ✅ От 200₽/месяц
- ✅ Полный контроль над сервером

**Альтернатива: Selectel или Beget**
- Также российские провайдеры
- Хорошая техподдержка
- Принимают российские карты

**Для быстрой установки используйте скрипт `setup.sh`:**
```bash
# На сервере выполните:
chmod +x setup.sh
sudo ./setup.sh
```

---

## Быстрая инструкция для Timeweb:

1. Зарегистрируйтесь на timeweb.com/cloud
2. Создайте VPS (Ubuntu 22.04, минимум 1GB RAM)
3. Подключитесь по SSH
4. Загрузите файлы проекта (через git или scp)
5. Запустите `sudo ./setup.sh`
6. Готово! Бот работает 24/7

